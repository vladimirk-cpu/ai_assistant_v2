# Secrets
.env
.env.example
# (за исключением .env.example, если мы хотим его оставить для структуры, но .env — точно игнорировать)

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script, so they may be in the same directory as the script, or a separate directory.
*.manifest
*.spec

# Installer
installer/Output/

# Logs
logs/
*.log

# Environments
.venv
venv
ENV

# IDEs
.vscode/
.idea/
.gemini/
*.sublime-project
*.sublime-workspace

# Local workspace
workspace/
memory-bank/
